# MetaLab
a MacOS ExifTool GUI
MetaLab is a free macOS EXIF editor.
It is designed as a local desktop tool: import images, inspect metadata, edit fields, apply reusable templates, adjust GPS information, rename files before saving, and export changes either in place or as copies.

# Features
-Import individual images or folders
-Read and edit EXIF metadata
-Batch-save metadata changes
-Save in place or save as copies
-Rename photos before saving
-Keep file extensions protected while renaming
-Edit GPS coordinates with map support
-Apply reusable camera, lens, digitization, and credit templates
-Small-thumbnail and no-thumbnail file navigation modes
-Local processing with bundled ExifTool

# Download
-Download the latest DMG from the Releases page.

# Current release package:
MetaLab-1.0-macOS-14.0-universal.dmg

# System Requirements：
！！only tested on macOS 26.4！！
Designed for macOS 14.0 or later
Apple Silicon or Intel Mac (only tested on Apple Silicon！！)
This free DMG build is not notarized by Apple.

# To open MetaLab for the first time:
Drag the 'MetaLab.app' to the applications folder.
Right-click 'MetaLab.app' .
Choose 'Open'.
Confirm 'Open' again if macOS asks.
Privacy:
MetaLab is a local macOS application.

# Selected files are processed on your Mac.
MetaLab does not upload your photos, files, or EXIF metadata to a MetaLab server.
MetaLab does not include analytics, advertising tracking, or telemetry.
GPS and map-related features may use Apple system frameworks such as MapKit, CoreLocation, and geocoding services.
ExifTool is bundled and runs locally for metadata reading and writing.

# Safety Notice
Before batch editing important images, keep a backup of the original files.
Metadata editing can modify files directly when using in-place save mode.

# Third-Party Components
MetaLab includes ExifTool by Phil Harvey.
ExifTool website:
https://exiftool.org/
ExifTool is distributed under its own licensing terms. Please refer to ExifTool’s documentation for details.

# Release Notes
1.0
-Initial public DMG release
-Universal macOS build
-EXIF reading and editing
-Batch save support
-GPS map editor
-Metadata templates
-Rename-before-save workflow

